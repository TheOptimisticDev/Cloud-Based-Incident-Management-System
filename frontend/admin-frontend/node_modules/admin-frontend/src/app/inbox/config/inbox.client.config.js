'use strict';

// Configuring the Inbox module
angular.module('inbox').run(['Menus',
	function(Menus) {
		// Add the Inbox item
		Menus.addMenuItem('topbar', {
			title: 'Inbox',
			state: 'inbox.ticket({ticketId:""})',
            roles: ['receptionist', 'admin'],
            position: -1
        });
	}
]);
