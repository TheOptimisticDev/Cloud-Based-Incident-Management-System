'use strict';

// Configuring the Post bookings module
angular.module('post-bookings').run(['Menus',
	function(Menus) {
		 //Add the Post bookings dropdown item
		Menus.addMenuItem('topbar', {
		 	title: 'Post-Booking',
			state: 'postbookings',
			roles: ['post-booking', 'admin'],
			position: 4
		 });
	}
]);
