'use strict';

// Reservations controller
angular.module('reservations').controller('ReservationsController', [
    '$scope',
    'Menus',
    function($scope, Menus) {
        // Set global variables
        $scope.sidebar = Menus.getMenu('reservations');
    }
]);
