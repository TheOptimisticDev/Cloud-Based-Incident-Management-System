'use strict';

// Reservations controller
angular.module('reservations').controller('PendingRequestsController', ['$scope', 'Reservations',
    function($scope, Reservations) {
        $scope.requests = Reservations.pendingRequests();
    }
]);
